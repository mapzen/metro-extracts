machine:
  node:
    version: 4.2

dependencies:
  pre:
    - sudo pip install -U awscli
  post:
    - make

test:
  override:
    - echo 'No tests!'

general:
  artifacts:
    - "dist"

deployment:
  production:
    branch: master
    commands:
      - ./deploy dev
      - ./deploy prod
